[project]
name = "encoder"
version = "0.1.0"
description = "SPLADE encoder service for rag-cosense"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "uvicorn",
    "transformers",
    "torch",
    "fugashi",
    "ipadic",
    "unidic-lite",
    "pydantic",
]
